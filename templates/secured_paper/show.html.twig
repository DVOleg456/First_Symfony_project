{% extends 'base.html.twig' %}

{% block title %}SecuredPaper{% endblock %}

{% block body %}
    <h1>SecuredPaper</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ secured_paper.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ secured_paper.name }}</td>
            </tr>
            <tr>
                <th>Secured_paper_type</th>
                <td>{{ secured_paper.securedPaperType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_secured_paper_index') }}">back to list</a>

    <a href="{{ path('app_secured_paper_edit', {'id': secured_paper.id}) }}">edit</a>

    {{ include('secured_paper/_delete_form.html.twig') }}
{% endblock %}
