<form method="post" action="{{ path('app_secured_paper_delete', {'id': secured_paper.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secured_paper.id) }}">
    <button class="btn">Delete</button>
</form>
